name: Refresh merged-provider host lock

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  refresh:
    if: github.event.pull_request.head.ref == 'refactor/standalone-devenv-maintenance'
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v5
        with:
          ref: refactor/standalone-devenv-maintenance
          fetch-depth: 0
      - uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
            accept-flake-config = true
      - name: Regenerate flake and lock
        run: |
          set -euo pipefail
          nix run .#write-flake
          nix flake lock --accept-flake-config
          if git diff --quiet -- flake.nix flake.lock; then
            exit 0
          fi
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add flake.nix flake.lock
          git commit -m "chore: repin merged providers"
          git push origin HEAD:refactor/standalone-devenv-maintenance
